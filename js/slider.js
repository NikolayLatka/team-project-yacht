$(function () {
  $('').slick({
    arrows: false,
    dots: true,
    fade: true,
    autoplay: true,
    autoplaySpeed: 3000,
  });
});

$(function () {
  $('.rewiew-list').slick({
    arrows: false,
    dots: true,
    fade: true,
    autoplay: false,
    autoplaySpeed: 3000,
  });
});
